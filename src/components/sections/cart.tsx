import React from "react";
import { Box } from "@chakra-ui/react";
import CartCard from "./cartCard";

const Cart = () => {
  return (
    <Box border="1px">
      <CartCard />
    </Box>
  );
};

export default Cart;
